# Implementation Plan Overview

This `docs` folder holds the phased plan for hardening and containerising the **gemsAPI** app without installing anything on the host. Each phase includes objectives, deliverables, and validation steps. Phase 1 (below) covers single-container dockerisation behind Nginx Proxy Manager on the shared Docker network `shared_net`.
